apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - clusterrole.yaml
  - clusterrolebinding.yaml
  - configmap.yaml
  - deployment.yaml

commonLabels:
  app.kubernetes.io/name: adot-amp-collector
  app.kubernetes.io/component: observability
  app.kubernetes.io/part-of: nexoraa-platform
